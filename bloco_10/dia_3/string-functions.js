const toUpperCase = (string) => {
  return string.toUpperCase();
}

const returnFirstLetter = (string) => {
  return string[0];
}

const concatStrings = (string1, string2) => {
  return string1 + string2;
}

module.exports = { toUpperCase, returnFirstLetter, concatStrings }
